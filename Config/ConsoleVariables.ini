; Shader iteration settings for this project.
; ShaderDevelopmentMode surfaces shader compile errors in the log instead of
; silently falling back, and makes "recompileshaders changed" pick up edits to
; /Project/*.ush without restarting the editor.

[Startup]
r.ShaderDevelopmentMode=1
r.DumpShaderDebugInfo=0
